## 2026-03-25 - [Standardizing Accessible Dropdowns]
**Learning:** Custom hover-based dropdown menus in this repository (using `group-hover`) are inaccessible for keyboard and screen reader users and inconsistent on mobile.
**Action:** Always prefer standard shadcn/ui `DropdownMenu` (click-based) over custom hover-based implementations to ensure full accessibility and mobile compatibility.
